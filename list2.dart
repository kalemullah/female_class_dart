void main() {
  List<String> Names = ['sapna', 'baji', 'Najeeba', 'fyp', 'css', 'DBMS'];

  for (int i = 0; i < Names.length; i++) {
    // print("${Names[i]}");
    // Names[i]%   
  }

  // List<String> numbers2 = ['fyp', 'css', 'DBMS'];
  // numbers.addAll(numbers2);
  // numbers.removeAt(3);
  // print(numbers);

  // print('this is list ${numbers.first}');
  // print('this is list ${numbers[numbers.length - 1]}');
  // print('this is list ${numbers.isEmpty}');
  // print('this is list ${numbers.isNotEmpty}');
  // print('this is list ${numbers.length}');
  // print('this is list ${numbers.reversed}');
}
